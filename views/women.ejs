<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Women's Collection</title>
    <link rel="stylesheet" href="/css/style.css">
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="shortcut icon" href="/images/logo.png" type="image/x-icon">
</head>
<body class="text-[18px]">
    <div>
        <%- include('utils/nav') %>

        <div class="flex justify-center items-center w-full  mt-6 ">
            <img src="/images/homepage_banner_copy_1_OsQCGcf.webp" alt="" class="w-[89vw] object-cover">
        </div>

        <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-5 p-6 lg:p-20">
            <% for (let product of women) { %>
            <div class="m-4 bg-white p-4 rounded-lg shadow-md">
              <a href="/women/<%= product._id %>">
                <img src="<%= product.image %>" alt="<%= product.name %>" class="w-full h-auto object-cover rounded-lg" />
                <p class="mt-2 font-medium text-center"><%= product.name %></p>
                <div class="flex justify-center gap-4 mt-2">
                  <p class="font-semibold">₹ <%= product.new_price %></p>
                  <% if (product.old_price) { %>
                  <p class="text-red-500 line-through text-gray-500"> ₹ <%= product.old_price %></p>
                  <% } %>
                </div>
              </a>
            </div>
            <% } %>
          </div>

        
    <%- include('utils/footer')%>
    </div>
</body>
</html>