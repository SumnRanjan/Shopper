<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= product.name %></title>
    <link rel="stylesheet" href="/css/style.css">
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="shortcut icon" href="/images/logo.png" type="image/x-icon">
</head>
<body class="font-mono text-[18px] ">
    <div class="container mx-auto p-4">
        <%- include('utils/nav') %>
        <div class="flex flex-col lg:flex-row mt-8 bg-white shadow-lg rounded-lg overflow-hidden ">
            <div class="flex justify-center items-center p-4 lg:w-1/2">
                <img src="<%= product.image %>" alt="<%= product.name %>" class="object-cover w-full h-auto rounded-lg shadow-md" height="400" width="500">
            </div>

            <div class="p-6 lg:w-1/2">
                <h1 class="text-3xl font-semibold mb-4"><%= product.name %></h1>
                <p class="text-xl font-bold mb-2">Price : <span class="text-green-600"> ₹ <%= product.new_price %></span></p>
                <% if (product.old_price) { %>
                    <p class="text-xl mb-4 line-through text-gray-500">Was : ₹ <%= product.old_price %></p>
                <% } %>
                <p class="text-lg mb-4">Category: <span class="font-medium text-gray-700"><%= product.category %></span></p>
                <p class="text-base mb-6 text-gray-700"><%= product.description %></p>
                
                <form action="/cart/add" method="POST" class="flex flex-col gap-4">
                    <input type="hidden" name="productId" value="<%= product._id %>">
                    <button type="submit" class="bg-green-500 text-white py-3 px-6 rounded-lg font-semibold hover:bg-green-600 transition-colors focus:outline-none focus:ring-2 focus:ring-green-500 focus:ring-opacity-50">Add to Cart</button>
                </form>
            </div>
        </div>
    </div>
</body>
</html>
